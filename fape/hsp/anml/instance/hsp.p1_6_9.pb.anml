// instance1_6_9

instance Hoist h0;
instance Position p0, p1, p2, p3, p4, p5;
instance Bar b0, b1, b2, b3, b4, b5, b6, b7, b8;

h0.can_go(p0) := true;
h0.can_go(p1) := true;
h0.can_go(p2) := true;
h0.can_go(p3) := true;
h0.can_go(p4) := true;
h0.can_go(p5) := true;



[start] {
	b0.at := void;
	b1.at := void;
	b2.at := void;
	b3.at := void;
	b4.at := void;
	b5.at := void;
	b6.at := void;
	b7.at := void;
	b8.at := void;

    
	h0.at := p0;
    

	h0.free := true;


	p0.free := true;
	p1.free := true;
	p2.free := true;
	p3.free := true;
	p4.free := true;
	p5.free := true;
    

	plant_loading_position := p0;
	plant_unloading_position := p5;
};

/*
[end] {
	b0.processed(p0) == true;
	b1.processed(p0) == true;
	b2.processed(p0) == true;
	b3.processed(p0) == true;
	b4.processed(p0) == true;
	b5.processed(p0) == true;
	b6.processed(p0) == true;
	b7.processed(p0) == true;
	b8.processed(p0) == true;
	b0.processed(p1) == true;
	b1.processed(p1) == true;
	b2.processed(p1) == true;
	b3.processed(p1) == true;
	b4.processed(p1) == true;
	b5.processed(p1) == true;
	b6.processed(p1) == true;
	b7.processed(p1) == true;
	b8.processed(p1) == true;
	b0.processed(p2) == true;
	b1.processed(p2) == true;
	b2.processed(p2) == true;
	b3.processed(p2) == true;
	b4.processed(p2) == true;
	b5.processed(p2) == true;
	b6.processed(p2) == true;
	b7.processed(p2) == true;
	b8.processed(p2) == true;
	b0.processed(p3) == true;
	b1.processed(p3) == true;
	b2.processed(p3) == true;
	b3.processed(p3) == true;
	b4.processed(p3) == true;
	b5.processed(p3) == true;
	b6.processed(p3) == true;
	b7.processed(p3) == true;
	b8.processed(p3) == true;
	b0.processed(p4) == true;
	b1.processed(p4) == true;
	b2.processed(p4) == true;
	b3.processed(p4) == true;
	b4.processed(p4) == true;
	b5.processed(p4) == true;
	b6.processed(p4) == true;
	b7.processed(p4) == true;
	b8.processed(p4) == true;
    
};
*/

[all] contains {
    	step(b0, p1);
	step(b1, p1);
	step(b2, p1);
	step(b3, p1);
	step(b4, p1);
	step(b5, p1);
	step(b6, p1);
	step(b7, p1);
	step(b8, p1);
	step(b0, p2);
	step(b1, p2);
	step(b2, p2);
	step(b3, p2);
	step(b4, p2);
	step(b5, p2);
	step(b6, p2);
	step(b7, p2);
	step(b8, p2);
	step(b0, p3);
	step(b1, p3);
	step(b2, p3);
	step(b3, p3);
	step(b4, p3);
	step(b5, p3);
	step(b6, p3);
	step(b7, p3);
	step(b8, p3);
	step(b0, p4);
	step(b1, p4);
	step(b2, p4);
	step(b3, p4);
	step(b4, p4);
	step(b5, p4);
	step(b6, p4);
	step(b7, p4);
	step(b8, p4);
    
};
